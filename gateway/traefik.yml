api:
  dashboard: true
  insecure: true  # dev only — remove in prod

entryPoints:
  web:
    address: ":80"

providers:
  docker:
    endpoint: "tcp://host.docker.internal:2375"
    exposedByDefault: false
    network: digicam-net

log:
  level: INFO
